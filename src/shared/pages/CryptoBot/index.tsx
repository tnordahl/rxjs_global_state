import React from 'react';
import { RouteComponentProps } from "@reach/router"
import CryptoContainer from '../../components/CryptoContainer';

let Home = (props: RouteComponentProps) => <CryptoContainer />

export default Home;
