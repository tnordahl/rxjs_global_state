import React from 'react';
import { RouteComponentProps } from "@reach/router"
import DebugBoxContainer from '../../components/DebugBoxContainer';

let Home = (props: RouteComponentProps) => <DebugBoxContainer />

export default Home;
